query CountryListQuery {
    countries: countries {
        code
        name
    }
}

query CountryDetailQuery($code: ID!) {
    country: country(code: $code) {
        code
        name
        continent {
            code
            name
        }
        capital
        emoji
    }
}